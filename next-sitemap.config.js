/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://heromenshealth.com',
  generateRobotsTxt: true,
  generateIndexSitemap: true,
  sitemapSize: 7000,
};

module.exports = config;